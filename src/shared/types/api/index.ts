/**
 * Contratos de API + schemas Zod para reutilização futura em package compartilhado (ex.: app mobile).
 * Exporte apenas deste barrel ao consumir de fora do monorepo.
 */
export * from './schemas';
