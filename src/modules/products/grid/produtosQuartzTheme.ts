import { themeQuartz } from 'ag-grid-community';

/**
 * Mesmo objeto de tema do exemplo publicado na documentação (`const theme = themeQuartz`).
 * @see https://www.ag-grid.com/javascript-data-grid/themes/
 */
export const produtosCatalogoQuartzTheme = themeQuartz;
